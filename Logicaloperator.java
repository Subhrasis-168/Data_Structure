// User function Template for Java
class logicaloperator {
    public static void operators(boolean a, boolean b) {
        // Do a AND b below
        boolean p = (a && b);
            // Do a OR b below
            boolean q =(a || b);
                // Do NOT a below
            boolean r = (!a);
            System.out.println(p + " " + q + " " + r);
    }
}